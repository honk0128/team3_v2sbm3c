<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.recommend.RecDAOInter">
  <!-- <insert id="rec_create" parameterType="dev.mvc.recommend.RecDAOInter">
    INSERT INTO recommend(recono, recocnt, recodate, boardno, accountno)
    VALUES(recommend_seq.nextval, #{recocnt}, sysdate, #{boardno}, #{accountno});
  </insert> -->

  <update id="reco_up" parameterType="Integer">
    UPDATE recommend
    SET reccnt = reccnt - 1
    WHERE recono = #{recono}
  </update>

  <update id="reco_down" parameterType="Integer">
    UPDATE recommend
    SET reccnt = reccnt + 1
    WHERE recono = #{recono}
  </update>
</mapper>